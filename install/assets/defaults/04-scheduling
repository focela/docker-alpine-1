#!/command/with-contenv bash
#------------------------------------------------------------------------
# Scheduling Configuration
#
# Purpose: Sets default environment variables for cron scheduling service
# logging configuration. This script establishes baseline settings for
# how scheduled task output and logging are handled within the container.
#
# Note: This script uses the ${VAR:-"default"} pattern to set defaults
# only if the variables are not already defined in the environment.
# All values can be overridden at container runtime.
#------------------------------------------------------------------------

#------------------------------------------------------------------------
# LOGGING CONFIGURATION
#------------------------------------------------------------------------
# Log output destination type
SCHEDULING_LOG_TYPE=${SCHEDULING_LOG_TYPE:-"FILE"}

# Directory path for cron log files
SCHEDULING_LOG_LOCATION=${SCHEDULING_LOG_LOCATION:-"/var/log/cron/"}

# Syslog priority level for cron messages (8 = info level)
SCHEDULING_LOG_LEVEL=${SCHEDULING_LOG_LEVEL:-"8"}
