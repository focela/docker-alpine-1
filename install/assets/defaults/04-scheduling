#!/command/with-contenv bash
#-----------------------------------------------------------------------------
# Scheduling Initialization Script
#
# Purpose: Configures logging settings for cron/scheduling operations
# Context: Runs in s6-overlay environment as part of scheduling setup sequence
# Note: This script prepares logging configuration for scheduled task execution
#-----------------------------------------------------------------------------

#-----------------------------------------------------------------------------
# CONFIGURATION
#-----------------------------------------------------------------------------
# Logging method for cron operations (FILE, SYSLOG, etc.)
SCHEDULING_LOG_TYPE=${SCHEDULING_LOG_TYPE:-"FILE"}

# Directory path for cron log files when using FILE logging
SCHEDULING_LOG_LOCATION=${SCHEDULING_LOG_LOCATION:-"/var/log/cron/"}

# Log verbosity level - 8 is debug level (highest verbosity) in syslog standards
SCHEDULING_LOG_LEVEL=${SCHEDULING_LOG_LEVEL:-"8"}
