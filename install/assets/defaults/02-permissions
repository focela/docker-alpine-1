#!/command/with-contenv bash
#------------------------------------------------------------------------
# Permissions Configuration
#
# Purpose: Sets debugging configuration for permission management operations.
# This script runs early in the s6-overlay initialization sequence to
# establish debugging settings before permission-related services start.
#
# Note: This script uses the ${VAR:-"default"} pattern to set defaults
# only if the variables are not already defined in the environment.
# All values can be overridden at container runtime.
#------------------------------------------------------------------------

#------------------------------------------------------------------------
# CONFIGURATION
#------------------------------------------------------------------------
# Debugging configuration for permission operations
DEBUG_PERMISSIONS=${DEBUG_PERMISSIONS:-"FALSE"}
